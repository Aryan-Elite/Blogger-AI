import { BlogGenerationLoader } from "../blog-generation-loader";

export default function BlogGenerationLoaderExample() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background p-4">
      <BlogGenerationLoader />
    </div>
  );
}
